# NetflixClone
A Netflix-inspired movie and TV series search engine built with React, ASP.NET 10, EF Core, and SQL Server. The application imports data from TMDB, stores it locally, and uses a custom search and recommendation algorithm instead of relying on external APIs.
